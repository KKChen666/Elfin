# Companion (伴伴) — Cursor Rules

> 本文件让 Cursor 自动遵循 Companion CDS v1.0 规范。

## 项目概述

Companion（伴伴）是一个温暖的关系守护者App，基于 React 18 + TypeScript + Vite + TailwindCSS + Zustand + Capacitor Android。

## 核心规则

### 不能做的（红线）
- 不能破坏 Design System 的颜色、间距、圆角、阴影
- 不能修改 Design Token（如 #E8734A 主色）
- 不能新增未命名的颜色值
- 不能写重复组件（检查 shared/components/ 是否已有）
- 不能使用 any 类型
- 不能硬编码颜色值（必须用 Tailwind 类名）

### 必须做的
- 新增页面必须响应式（Mobile + Desktop）
- 所有组件必须支持深色模式（dark: 类名）
- 所有 SVG 必须可组合（组件化）
- 动画使用 Lottie（复杂）或 CSS transition（简单）
- 点击区域最小 44×44px
- 文案必须温暖友好（不说教、不机械）
- 数据本地存储（localStorage）

### 设计令牌速查
- 主色: #E8734A (primary)
- 间距: 8pt grid (4,8,12,16,20,24,32,40,48px)
- 圆角: 按钮12px, 输入框12px, 卡片20px, 头像999px, 模态框24px
- 阴影: shadow-md (最大alpha 0.05)
- 字号: 正文16px, 辅助14px, 标注12px

### 组件规范
- 按钮高度: 48px, 圆角12px
- 输入框高度: 52px, 圆角12px
- 标签高度: 28px, 圆角999px
- 列表项高度: 56px

### 文件规范
- 组件: PascalCase.tsx
- Hook: useXxx.ts
- 工具: camelCase.ts
- 类型: index.ts

### 响应式断点
- Mobile: 0-640px (底部Tab导航)
- Tablet: 641-1024px (底部Tab宽屏)
- Desktop: 1025px+ (左侧侧边栏)

### 路由表
- / → Home (亲友广场)
- /add → AddRelative
- /edit/:id → EditRelative
- /detail/:id → Detail
- /avatar/:id → AvatarCustom
- /import/:id → ChatImport
- /chat/:id → Chat
- /reminders → Reminders
- /calendar → Calendar
- /stats → Stats

## 开发流程

1. 先阅读 `CDS/` 目录下相关模块规范
2. 遵循编码规范（CDS/40_Frontend/41_Coding_Convention.md）
3. 使用设计系统中的令牌和组件
4. 确保深色模式和响应式支持
5. 测试所有交互状态

## 更多规范

详见 `CDS/` 目录完整规范文档。
